import type { DesignResumeDocument, PdfRenderer } from "@shared/types";
import { PDF_RENDERER_LABELS } from "@shared/types";
import { Eye } from "lucide-react";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { DesignResumePdfPreview } from "./DesignResumePdfPreview";

type DesignResumePreviewPanelProps = {
  draft: DesignResumeDocument;
  pdfRenderer: PdfRenderer;
  isUpdatingRenderer: boolean;
  isDirty: boolean;
  saveState: "idle" | "saving" | "saved" | "error";
  onPdfRendererChange: (renderer: PdfRenderer) => void;
};

export function DesignResumePreviewPanel({
  draft,
  pdfRenderer,
  isUpdatingRenderer,
  isDirty,
  saveState,
  onPdfRendererChange,
}: DesignResumePreviewPanelProps) {
  return (
    <section class名称="flex min-h-0 flex-col overflow-hidden rounded-2xl border border-border/70 bg-muted/20">
      <div class名称="flex flex-wrap items-start justify-between gap-4 border-b border-border/70 px-4 py-4">
        <div class名称="min-w-0 flex-1">
          <div class名称="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.18em] text-muted-foreground">
            <Eye class名称="h-4 w-4" />
            Live preview
          </div>
          <p class名称="mt-1 text-xs text-muted-foreground">
            This shows the actual PDF generated by the selected template.
          </p>
        </div>

        <div class名称="grid w-full gap-2 sm:max-w-[17rem]">
          <label
            htmlFor="design-resume-template"
            class名称="text-[11px] font-medium uppercase tracking-[0.22em] text-muted-foreground"
          >
            Template
          </label>
          <Select
            value={pdfRenderer}
            onValueChange={(value) =>
              onPdfRendererChange(value === "latex" ? "latex" : "rxresume")
            }
            disabled={isUpdatingRenderer}
          >
            <SelectTrigger id="design-resume-template">
              <SelectValue placeholder="Choose a template" />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="rxresume">
                {PDF_RENDERER_LABELS.rxresume}
              </SelectItem>
              <SelectItem value="latex">{PDF_RENDERER_LABELS.latex}</SelectItem>
            </SelectContent>
          </Select>
        </div>
      </div>

      <div class名称="min-h-0 flex-1 overflow-hidden">
        <DesignResumePdfPreview
          draft={draft}
          pdfRenderer={pdfRenderer}
          isUpdatingRenderer={isUpdatingRenderer}
          isDirty={isDirty}
          saveState={saveState}
        />
      </div>
    </section>
  );
}
